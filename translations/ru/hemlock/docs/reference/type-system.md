# Справочник по системе типов

Полный справочник по системе типов Hemlock, включая все примитивные и составные типы.

---

## Обзор

Hemlock использует **динамическую систему типов** с тегами типов времени выполнения и необязательными аннотациями типов. Каждое значение имеет тип времени выполнения, и преобразования типов следуют явным правилам продвижения.

**Ключевые особенности:**
- Проверка типов времени выполнения (интерпретатор)
- Проверка типов времени компиляции (hemlockc - включена по умолчанию)
- Необязательные аннотации типов
- Автоматический вывод типов для литералов
- Явные правила продвижения типов
- Нет неявных преобразований с потерей точности

---

## Проверка типов времени компиляции (hemlockc)

Компилятор Hemlock (`hemlockc`) включает проверку типов времени компиляции, которая валидирует ваш код перед генерацией исполняемых файлов. Это позволяет обнаружить ошибки типов на ранней стадии без необходимости запускать программу.

### Поведение по умолчанию

Проверка типов **включена по умолчанию** в hemlockc:

```bash
# Проверка типов происходит автоматически
hemlockc program.hml -o program

# Ошибки сообщаются перед компиляцией
hemlockc bad_types.hml
# Вывод: 1 type error found
```

### Флаги компилятора

| Флаг | Описание |
|------|----------|
| `--check` | Только проверить типы, не компилировать (выход после валидации) |
| `--no-type-check` | Отключить проверку типов (не рекомендуется) |
| `--strict-types` | Включить более строгие предупреждения о типах |

**Примеры:**

```bash
# Только валидировать типы без компиляции
hemlockc --check program.hml
# Вывод: program.hml: no type errors

# Отключить проверку типов (используйте с осторожностью)
hemlockc --no-type-check dynamic_code.hml -o program

# Включить строгие предупреждения для неявных типов any
hemlockc --strict-types program.hml -o program
```

### Что проверяет проверщик типов

1. **Аннотации типов** - Убеждается, что присваиваемые значения соответствуют объявленным типам
2. **Вызовы функций** - Валидирует типы аргументов против типов параметров
3. **Возвращаемые типы** - Проверяет, что операторы return соответствуют объявленному возвращаемому типу
4. **Использование операторов** - Проверяет совместимость операндов
5. **Доступ к свойствам** - Валидирует типы полей объектов для типизированных объектов

### Разрешительные числовые преобразования

Проверщик типов разрешает числовые преобразования типов во время компиляции, с валидацией диапазона во время выполнения:

```hemlock
let x: i8 = 100;      // OK - 100 помещается в i8 (валидируется во время выполнения)
let y: u8 = 255;      // OK - в пределах диапазона u8
let z: f64 = 42;      // OK - i32 в f64 безопасно
```

### Поддержка динамического кода

Код без аннотаций типов рассматривается как динамический (тип `any`) и всегда проходит проверку типов:

```hemlock
let x = get_value();  // Динамический - без аннотации
process(x);           // OK - динамические значения принимаются везде
```

---

## Примитивные типы

### Числовые типы

#### Знаковые целые

| Тип    | Размер  | Диапазон                                       | Псевдоним |
|--------|---------|------------------------------------------------|-----------|
| `i8`   | 1 байт  | -128 до 127                                    | -         |
| `i16`  | 2 байта | -32,768 до 32,767                              | -         |
| `i32`  | 4 байта | -2,147,483,648 до 2,147,483,647                | `integer` |
| `i64`  | 8 байт  | -9,223,372,036,854,775,808 до 9,223,372,036,854,775,807 | - |

**Примеры:**
```hemlock
let a: i8 = 127;
let b: i16 = 32000;
let c: i32 = 1000000;
let d: i64 = 9223372036854775807;

// Псевдоним типа
let x: integer = 42;  // То же что i32
```

#### Беззнаковые целые

| Тип    | Размер  | Диапазон                          | Псевдоним |
|--------|---------|-----------------------------------|-----------|
| `u8`   | 1 байт  | 0 до 255                          | `byte`    |
| `u16`  | 2 байта | 0 до 65,535                       | -         |
| `u32`  | 4 байта | 0 до 4,294,967,295                | -         |
| `u64`  | 8 байт  | 0 до 18,446,744,073,709,551,615   | -         |

**Примеры:**
```hemlock
let a: u8 = 255;
let b: u16 = 65535;
let c: u32 = 4294967295;
let d: u64 = 18446744073709551615;

// Псевдоним типа
let byte_val: byte = 65;  // То же что u8
```

#### Числа с плавающей точкой

| Тип    | Размер  | Точность       | Псевдоним |
|--------|---------|----------------|-----------|
| `f32`  | 4 байта | ~7 цифр        | -         |
| `f64`  | 8 байт  | ~15 цифр       | `number`  |

**Примеры:**
```hemlock
let pi: f32 = 3.14159;
let precise: f64 = 3.14159265359;

// Псевдоним типа
let x: number = 2.718;  // То же что f64
```

---

### Вывод типа целочисленных литералов

Целочисленные литералы автоматически типизируются на основе их значения:

**Правила:**
- Значения в диапазоне i32 (-2,147,483,648 до 2,147,483,647): выводится `i32`
- Значения вне диапазона i32, но в диапазоне i64: выводится `i64`
- Используйте явные аннотации типов для других типов (i8, i16, u8, u16, u32, u64)

**Примеры:**
```hemlock
let small = 42;                    // i32 (помещается в i32)
let large = 5000000000;            // i64 (> макс. i32)
let max_i64 = 9223372036854775807; // i64 (INT64_MAX)
let explicit: u32 = 100;           // u32 (аннотация типа переопределяет)
```

---

### Булев тип

**Тип:** `bool`

**Значения:** `true`, `false`

**Размер:** 1 байт (внутренне)

**Примеры:**
```hemlock
let is_active: bool = true;
let done = false;

if (is_active && !done) {
    print("работаем");
}
```

---

### Символьные типы

#### Руна

**Тип:** `rune`

**Описание:** Кодовая точка Unicode (U+0000 до U+10FFFF)

**Размер:** 4 байта (32-битное значение)

**Диапазон:** 0 до 0x10FFFF (1,114,111)

**Синтаксис литерала:** Одинарные кавычки `'x'`

**Примеры:**
```hemlock
// ASCII
let a = 'A';
let digit = '0';

// Многобайтовый UTF-8
let rocket = '🚀';      // U+1F680
let heart = '❤';        // U+2764
let chinese = '中';     // U+4E2D

// Escape-последовательности
let newline = '\n';
let tab = '\t';
let backslash = '\\';
let quote = '\'';
let null = '\0';

// Unicode escape
let emoji = '\u{1F680}';   // До 6 hex цифр
let max = '\u{10FFFF}';    // Максимальная кодовая точка
```

**Преобразования типов:**
```hemlock
// Целое в руну
let code: rune = 65;        // 'A'
let r: rune = 128640;       // 🚀

// Руна в целое
let value: i32 = 'Z';       // 90

// Руна в строку
let s: string = 'H';        // "H"

// u8 в руну
let byte: u8 = 65;
let rune_val: rune = byte;  // 'A'
```

**См. также:** [API строк](string-api.md) для конкатенации строка + руна

---

### Строковый тип

**Тип:** `string`

**Описание:** UTF-8 кодированный, изменяемый, размещаемый в куче текст

**Кодировка:** UTF-8 (U+0000 до U+10FFFF)

**Изменяемость:** Изменяемый (в отличие от большинства языков)

**Свойства:**
- `.length` - Количество кодовых точек (количество символов)
- `.byte_length` - Количество байтов (размер кодировки UTF-8)

**Синтаксис литерала:** Двойные кавычки `"текст"`

**Примеры:**
```hemlock
let s = "hello";
s[0] = 'H';             // Изменение (теперь "Hello")
print(s.length);        // 5 (количество кодовых точек)
print(s.byte_length);   // 5 (байты UTF-8)

let emoji = "🚀";
print(emoji.length);        // 1 (одна кодовая точка)
print(emoji.byte_length);   // 4 (четыре байта UTF-8)
```

**Индексация:**
```hemlock
let s = "hello";
let ch = s[0];          // Возвращает руну 'h'
s[0] = 'H';             // Установить руной
```

**См. также:** [API строк](string-api.md) для полного справочника по методам

---

### Тип Null

**Тип:** `null`

**Описание:** Значение null (отсутствие значения)

**Размер:** 8 байт (внутренне)

**Значение:** `null`

**Примеры:**
```hemlock
let x = null;
let y: i32 = null;  // ОШИБКА: несоответствие типов

if (x == null) {
    print("x равен null");
}
```

---

## Составные типы

### Тип массива

**Тип:** `array`

**Описание:** Динамический, размещаемый в куче, массив смешанных типов

**Свойства:**
- `.length` - Количество элементов

**Индексация с нуля:** Да

**Синтаксис литерала:** `[элем1, элем2, ...]`

**Примеры:**
```hemlock
let arr = [1, 2, 3, 4, 5];
print(arr[0]);         // 1
print(arr.length);     // 5

// Смешанные типы
let mixed = [1, "hello", true, null];
```

**См. также:** [API массивов](array-api.md) для полного справочника по методам

---

### Тип объекта

**Тип:** `object`

**Описание:** Объект в стиле JavaScript с динамическими полями

**Синтаксис литерала:** `{ поле: значение, ... }`

**Примеры:**
```hemlock
let person = { name: "Alice", age: 30 };
print(person.name);  // "Alice"

// Добавить поле динамически
person.email = "alice@example.com";
```

**Определения типов:**
```hemlock
define Person {
    name: string,
    age: i32,
    active?: bool,  // Необязательное поле
}

let p: Person = { name: "Bob", age: 25 };
print(typeof(p));  // "Person"
```

---

### Типы указателей

#### Сырой указатель (ptr)

**Тип:** `ptr`

**Описание:** Сырой адрес памяти (небезопасный)

**Размер:** 8 байт

**Проверка границ:** Нет

**Примеры:**
```hemlock
let p: ptr = alloc(64);
memset(p, 0, 64);
free(p);
```

#### Буфер (buffer)

**Тип:** `buffer`

**Описание:** Безопасная обёртка указателя с проверкой границ

**Структура:** Указатель + длина + ёмкость

**Свойства:**
- `.length` - Размер буфера
- `.capacity` - Выделенная ёмкость

**Примеры:**
```hemlock
let b: buffer = buffer(64);
b[0] = 65;              // С проверкой границ
print(b.length);        // 64
free(b);
```

**См. также:** [API памяти](memory-api.md) для функций выделения

---

## Специальные типы

### Тип файла

**Тип:** `file`

**Описание:** Дескриптор файла для операций ввода-вывода

**Свойства:**
- `.path` - Путь к файлу (string)
- `.mode` - Режим открытия (string)
- `.closed` - Закрыт ли файл (bool)

**См. также:** [API файлов](file-api.md)

---

### Тип задачи

**Тип:** `task`

**Описание:** Дескриптор параллельной задачи

**См. также:** [API параллелизма](concurrency-api.md)

---

### Тип канала

**Тип:** `channel`

**Описание:** Потокобезопасный канал коммуникации

**См. также:** [API параллелизма](concurrency-api.md)

---

### Тип функции

**Тип:** `function`

**Описание:** Значение функции первого класса

**Примеры:**
```hemlock
fn add(a, b) {
    return a + b;
}

let multiply = fn(x, y) {
    return x * y;
};

print(typeof(add));      // "function"
print(typeof(multiply)); // "function"
```

---

### Тип Void

**Тип:** `void`

**Описание:** Отсутствие возвращаемого значения (внутреннее использование)

---

## Правила продвижения типов

При смешивании типов в операциях Hemlock продвигает к "высшему" типу:

**Иерархия продвижения:**
```
f64 (высшая точность)
 ↑
f32
 ↑
u64
 ↑
i64
 ↑
u32
 ↑
i32
 ↑
u16
 ↑
i16
 ↑
u8
 ↑
i8 (низшая)
```

**Правила:**
1. Float всегда выигрывает над integer
2. Больший размер выигрывает в пределах той же категории (int/uint/float)
3. Оба операнда продвигаются к типу результата
4. **Сохранение точности:** i64/u64 + f32 продвигается к f64 (не f32)

**Примеры:**
```hemlock
// Продвижение по размеру
u8 + i32    → i32    // Больший размер выигрывает
i32 + i64   → i64    // Больший размер выигрывает
u32 + u64   → u64    // Больший размер выигрывает

// Продвижение к float
i32 + f32   → f32    // Float выигрывает, f32 достаточно для i32
i64 + f32   → f64    // Продвигается к f64 для сохранения точности i64
i64 + f64   → f64    // Float всегда выигрывает
i8 + f64    → f64    // Float + наибольший выигрывает
```

**Почему i64 + f32 → f64?**

f32 имеет только 24-битную мантиссу, которая не может точно представить целые числа больше 2^24 (16,777,216). Поскольку i64 может содержать значения до 2^63, смешивание i64 с f32 вызвало бы серьёзную потерю точности. Hemlock продвигает к f64 (53-битная мантисса) вместо этого.

---

## Проверка диапазона

Аннотации типов обеспечивают проверку диапазона при присваивании:

**Допустимые присваивания:**
```hemlock
let x: u8 = 255;             // OK
let y: i8 = 127;             // OK
let a: i64 = 2147483647;     // OK
let b: u64 = 4294967295;     // OK
```

**Недопустимые присваивания (ошибка времени выполнения):**
```hemlock
let x: u8 = 256;             // ОШИБКА: вне диапазона
let y: i8 = 128;             // ОШИБКА: максимум 127
let z: u64 = -1;             // ОШИБКА: u64 не может быть отрицательным
```

---

## Интроспекция типов

### typeof(value)

Возвращает имя типа как строку.

**Сигнатура:**
```hemlock
typeof(value: any): string
```

**Возвращает:**
- Примитивные типы: `"i8"`, `"i16"`, `"i32"`, `"i64"`, `"u8"`, `"u16"`, `"u32"`, `"u64"`, `"f32"`, `"f64"`, `"bool"`, `"string"`, `"rune"`, `"null"`
- Составные типы: `"array"`, `"object"`, `"ptr"`, `"buffer"`, `"function"`
- Специальные типы: `"file"`, `"task"`, `"channel"`
- Типизированные объекты: Имя пользовательского типа (напр., `"Person"`)

**Примеры:**
```hemlock
print(typeof(42));              // "i32"
print(typeof(3.14));            // "f64"
print(typeof("hello"));         // "string"
print(typeof('A'));             // "rune"
print(typeof(true));            // "bool"
print(typeof([1, 2, 3]));       // "array"
print(typeof({ x: 10 }));       // "object"

define Person { name: string }
let p: Person = { name: "Alice" };
print(typeof(p));               // "Person"
```

**См. также:** [Встроенные функции](builtins.md#typeof)

---

## Преобразования типов

### Неявные преобразования

Hemlock выполняет неявные преобразования типов в арифметических операциях, следуя правилам продвижения типов.

**Примеры:**
```hemlock
let a: u8 = 10;
let b: i32 = 20;
let result = a + b;     // result имеет тип i32 (продвинуто)
```

### Явные преобразования

Используйте аннотации типов для явных преобразований:

**Примеры:**
```hemlock
// Целое в float
let i: i32 = 42;
let f: f64 = i;         // 42.0

// Float в целое (усечение)
let x: f64 = 3.14;
let y: i32 = x;         // 3

// Целое в руну
let code: rune = 65;    // 'A'

// Руна в целое
let value: i32 = 'Z';   // 90

// Руна в строку
let s: string = 'H';    // "H"
```

---

## Псевдонимы типов

### Встроенные псевдонимы

Hemlock предоставляет встроенные псевдонимы типов для распространённых типов:

| Псевдоним | Фактический тип | Использование              |
|-----------|-----------------|----------------------------|
| `integer` | `i32`           | Целые общего назначения    |
| `number`  | `f64`           | Float общего назначения    |
| `byte`    | `u8`            | Байтовые значения          |

**Примеры:**
```hemlock
let count: integer = 100;       // То же что i32
let price: number = 19.99;      // То же что f64
let b: byte = 255;              // То же что u8
```

### Пользовательские псевдонимы типов

Определите пользовательские псевдонимы типов с помощью ключевого слова `type`:

```hemlock
// Простые псевдонимы
type Integer = i32;
type Text = string;

// Псевдонимы типов функций
type Callback = fn(i32): void;
type Predicate = fn(any): bool;
type BinaryOp = fn(i32, i32): i32;

// Псевдонимы составных типов
define HasName { name: string }
define HasAge { age: i32 }
type Person = HasName & HasAge;

// Обобщённые псевдонимы типов
type Pair<T> = { first: T, second: T };
type Result<T, E> = { value: T?, error: E? };
```

**Использование пользовательских псевдонимов:**
```hemlock
let cb: Callback = fn(n) { print(n); };
let p: Person = { name: "Alice", age: 30 };
let coords: Pair<f64> = { first: 3.14, second: 2.71 };
```

**Примечание:** Псевдонимы типов прозрачны - `typeof()` возвращает имя базового типа.

---

## Типы функций

Типы функций указывают сигнатуру значений функций:

### Синтаксис

```hemlock
fn(типы_параметров): тип_возврата
```

### Примеры

```hemlock
// Базовый тип функции
let add: fn(i32, i32): i32 = fn(a, b) { return a + b; };

// Параметр функции
fn apply(f: fn(i32): i32, x: i32): i32 {
    return f(x);
}

// Функция высшего порядка, возвращающая функцию
fn make_adder(n: i32): fn(i32): i32 {
    return fn(x) { return x + n; };
}

// Тип async функции
fn run_async(handler: async fn(): void) {
    spawn(handler);
}
```

---

## Составные типы (Пересечение)

Составные типы используют `&` для требования нескольких ограничений типа:

```hemlock
define HasName { name: string }
define HasAge { age: i32 }
define HasEmail { email: string }

// Объект должен удовлетворять всем типам
let person: HasName & HasAge = { name: "Alice", age: 30 };

// Три или более типов
fn describe(p: HasName & HasAge & HasEmail) {
    print(p.name + " <" + p.email + ">");
}
```

---

## Сводная таблица

| Тип        | Размер   | Изменяемый | В куче         | Описание                       |
|------------|----------|------------|----------------|--------------------------------|
| `i8`-`i64` | 1-8 байт | Нет        | Нет            | Знаковые целые                 |
| `u8`-`u64` | 1-8 байт | Нет        | Нет            | Беззнаковые целые              |
| `f32`      | 4 байта  | Нет        | Нет            | Float одинарной точности       |
| `f64`      | 8 байт   | Нет        | Нет            | Float двойной точности         |
| `bool`     | 1 байт   | Нет        | Нет            | Булев                          |
| `rune`     | 4 байта  | Нет        | Нет            | Кодовая точка Unicode          |
| `string`   | Перем.   | Да         | Да             | UTF-8 текст                    |
| `array`    | Перем.   | Да         | Да             | Динамический массив            |
| `object`   | Перем.   | Да         | Да             | Динамический объект            |
| `ptr`      | 8 байт   | Нет        | Нет            | Сырой указатель                |
| `buffer`   | Перем.   | Да         | Да             | Безопасная обёртка указателя   |
| `file`     | Непрозр. | Да         | Да             | Дескриптор файла               |
| `task`     | Непрозр. | Нет        | Да             | Дескриптор параллельной задачи |
| `channel`  | Непрозр. | Да         | Да             | Потокобезопасный канал         |
| `function` | Непрозр. | Нет        | Да             | Значение функции               |
| `null`     | 8 байт   | Нет        | Нет            | Значение null                  |

---

## См. также

- [Справочник по операторам](operators.md) - Поведение типов в операциях
- [Встроенные функции](builtins.md) - Интроспекция и преобразование типов
- [API строк](string-api.md) - Методы строкового типа
- [API массивов](array-api.md) - Методы типа массива
- [API памяти](memory-api.md) - Операции с указателями и буферами
